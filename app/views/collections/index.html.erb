<% content_for :javascript do %>
  <script type="text/javascript">
    ShopifyApp.ready(function(){
      ShopifyApp.Bar.initialize({
        title: "Home",
        icon: "<%= asset_path('favicon.ico') %>"
      });
    });
  </script>
<% end %>

<div id="collection_index">
  <h1>Collections</h1>
  <legend><%= link_to "Background", home_path %></legend>

      <!-- <div class="tbl-header">
        <table cellpadding="0" cellspacing="0">

          <thead>
            <tr class="th-row">
              <th>Title</th>
              <th>Filters</th>
              <th>Categories</th>
              <th>Types</th>
              <th>Tags</th>

              <th>Products</th>
              <th colspan="3"></th>

            </tr>
          </thead>

        </table>
      </div> -->
      <div class="table">
      <div class="tbl-content">
        <table cellpadding="0" cellspacing="0" border="0">

          <%- if @custom_collections.any? or @smart_collections.any? %>
          <tbody >

            <% @custom_collections.each do |collection| %>
              <tr class="row">
                <td class="collectionTitle"><%= link_to collection.title, categories_path(shopify_collection_id:collection.id, shopify_collection_title:collection.title) %></td>
               </tr>
  <!--               <td><button class="product_btn"><%# link_to "Filters", categories_path(shopify_collection_id:collection.id, shopify_collection_title:collection.title) %></button>  -->


               <!--  <td><%# collection.categories.count %>0</td>
                <td><%# collection.categories.count %>0</td> -->
                 <!-- <td><button class="product_btn"><%# link_to "Products", products_path(shopify_collection_id:collection.id, shopify_collection_title:collection.title) %></button></td>
   -->
            <% end %>
          </tbody>

            <%# paginate @custom_collections  %>



          <tbody>
            <% @smart_collections.each do |collection| %>
            <tr class="row">
                <td class="collectionTitle"><%= link_to collection.title, categories_path(shopify_collection_id:collection.id, shopify_collection_title:collection.title) %></td>
              </tr>
            <% end %>

            <%# paginate @smart_collections  %>
          <%- else %>

          <%- end %>
        </tbody>
      </table>

  </div>
  </div>
  <%- if @custom_collections.any? or @smart_collections.any? %>
    <div id="begin"><h2>Choose a collection to begin.</h2></div>
  <%- else %>
    <div id="begin"><h2>You should create these on shopify and then return.</h2></div>
  <%- end %>
</div>
