
// alert("<%= @category.inspect %>")
// DESELCT .TAGS        --> aelect back 
$("#form-element-<%= @product ?  @product.id : 'nil' %>").replaceWith("<%= j render('products/form_element', {product: @product, category: @category, product_type: @product_type, tags: @tags}) %>")
// $("#form-element-<%= @product ?  @product.id : 'nil' %>").css('background', "#ff0000");
//$("#form-element-<%# @product.id %>").replace("<%# render 'product/form_element', {} %>")

$('.chosen-select').chosen({
    no_results_text: 'No results matched',
    width: '120px',
    allow_single_deselect: true,
    overflow_hidden: false

});


$('tag')[0].options[1].setAttribute('selected','selected');

$('.tags').on('click', function(){
	$(this).setAttribute('selected','selected');
});